{
  "name": "wayne-ai-assistant",
  "version": "1.0.0",
  "description": "WAYNE AI Power Assistant",
  "main": "worker/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^3.15.0",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
